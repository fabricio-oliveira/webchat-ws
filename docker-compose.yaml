version: "3.8"
services:
  backend:
    build:
      context: ./services/backend/
      dockerfile: ./build/Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./services/backend/:/app
    restart: on-failure